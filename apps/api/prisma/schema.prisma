generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model AppConfig {
  id    Int    @id @default(autoincrement())
  key   String @unique
  value String
}
